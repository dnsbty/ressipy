<h2>Edit Ingredient</h2>

<%= render "form.html", changeset: @changeset,
                        action: ingredient_path(@conn, :update, @ingredient) %>

<span><%= link "Back", to: ingredient_path(@conn, :index) %></span>
