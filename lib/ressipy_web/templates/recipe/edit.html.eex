<h2>Edit Recipe</h2>

<%= render "form.html", changeset: @changeset,
                        categories: @categories,
                        action: Routes.recipe_path(@conn, :update, @recipe) %>

<span><%= link "Back", to: Routes.recipe_path(@conn, :show, @recipe.slug) %></span>
