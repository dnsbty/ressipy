<h2>Edit Category</h2>

<%= render "form.html", changeset: @changeset,
                        action: Routes.category_path(@conn, :update, @category.slug) %>

<span><%= link "Back", to: Routes.category_path(@conn, :index) %></span>
